<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyBuddy"
         >
        <activity
            android:name=".bathtub.LeaderboardActivity"
            android:label="@string/title_activity_leaderboard"
            android:exported="false" />
        <activity
            android:name=".bathtub.TimerActivity"
            android:label="@string/title_activity_timer"
            android:exported="false" />
        <activity
            android:name=".bathtub.SetTimeActivity"
            android:label="@string/title_activity_set_time"
            android:exported="false" />
        <activity
            android:name=".bathtub.SearchActivity"
            android:label="@string/title_activity_search"
            android:exported="false" />
        <activity
            android:name=".bathtub.AssessmentsActivity"
            android:label="@string/title_activity_assessments"
            android:exported="false" />

        <activity
            android:name=".bathtub.MainActivity"
            android:label="@string/title_activity_main"
            android:exported="true" />
        <activity
            android:name=".bathtub.RegisterActivity"
            android:label="@string/title_activity_register"
            android:exported="false" />
        <activity
            android:name=".bathtub.LoginActivity"
            android:label="@string/title_activity_login"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>